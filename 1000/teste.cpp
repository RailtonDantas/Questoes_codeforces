#include<iostream>
#include<vector>
#include<algorithm>
#include<cmath>
#define read_vec(v) for(auto& x : v) cin >> x;
typedef long long ll;


using namespace std;


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    
    
    return 0;
}