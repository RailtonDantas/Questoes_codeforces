#include<iostream>
#include<vector>
#include<algorithm>
#include<cmath>
#define read_vec(v,n) for(int i = 0; i < n; i++) cin >> v[i];

using namespace std;


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int c;
    cin >> c;

    
    while(c--){
        int n,k;
        cin >> n >> k;

        vector<int> seq(n),seq2(n);
        read_vec(seq,n);
        read_vec(seq2,n);


    }

    return 0;
}